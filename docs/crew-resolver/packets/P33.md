## Standing worker rules
- Work only inside `~/projects/lee-llm-router`. Never touch `~/projects/auto-orch`, `~/projects/chief-of-staff`, or any file outside the allowed list. Do not commit.
- No real provider prompt, ever. Do not run ai-subs or any provider CLI; use fixture snapshots under `tests/fixtures/`.
- Checks: `PYTHONPATH=src .venv/bin/python -m pytest -q` (baseline 557, must pass), `.venv/bin/black src tests/test_availability.py` then `.venv/bin/black --check src`, `.venv/bin/ruff check src`.
- If a test fails in a file you do not own, stop and report it; do not patch around it.
- Finish with a report: files touched, tests added, exact commands run and their last lines.

# Packet P33 — availability reader learns the `OpenCode/Go` provider label

You are the only worker editing this repository right now.

## Context
`ai-subs.sh` (the snapshot source) now emits a fourth provider, label `OpenCode/Go`, with three buckets: `Rolling — 5-hour` (5 h window), `Weekly` (7-day), `Monthly` (30-day). Example entries (real, 2026-09-07):
```
{"provider":"OpenCode/Go","bucket":"Rolling — 5-hour","status":"COLD","used_pct":1.0,"remaining_pct":99.0,"pace_ratio":0.1,"pace_ratio_infinite":false,"resets_at":"2026-09-08T02:20:21.278000-05:00","resets_in_hours":4.5}
{"provider":"OpenCode/Go","bucket":"Weekly","status":"COLD","used_pct":11.0,"remaining_pct":89.0,"pace_ratio":0.4,"pace_ratio_infinite":false,"resets_at":"2026-09-13T19:00:00.278000-05:00","resets_in_hours":140.0}
{"provider":"OpenCode/Go","bucket":"Monthly","status":"TOO FAST","used_pct":11.0,"remaining_pct":89.0,"pace_ratio":1.6,"pace_ratio_infinite":false,"resets_at":"2026-10-05T14:31:14.278000-05:00","resets_in_hours":660.0}
```
The channel already exists in `CHANNELS` as `opencode-go` and the five `opencode_go_*` crew workers already bill to it; today the reader returns no channel for this label, so the channel stays `unknown`.

## Allowed files (touch nothing else)
- `src/lee_llm_router/availability.py` — `channels_for` (and any label constant it uses)
- `tests/test_availability.py`
- `tests/fixtures/` — extend the existing availability fixture snapshot(s) with the three entries above (keep existing entries byte-identical)
- `docs/config.md` — the channel mapping table row for `opencode-go`

## Contract
1. `channels_for("OpenCode/Go", <any bucket>)` returns `("opencode-go",)`. All three buckets constrain the single channel (there is no second OpenCode channel). `is_single_channel` is therefore True for them.
2. Channel health for `opencode-go` follows exactly the same rule as the other channels (whatever the existing per-channel aggregation over buckets is — do not special-case). Note `resets_at` carries microseconds; the existing timestamp parser must accept it (add a test).
3. An unknown provider label still returns `()` (unchanged; add/keep a test).
4. Tests: mapping for each of the three bucket names; the fixture-driven snapshot reports `opencode-go` with a non-`unknown` health; microsecond `resets_at` parses.
5. Done: full suite green (≥ 557 + your new tests); black/ruff clean on src; paste the last lines.
