## Standing worker rules
- Work only inside `~/projects/lee-llm-router`. Do not commit. No provider CLI runs.
- Checks: `PYTHONPATH=src .venv/bin/python -m pytest -q` (must pass), `.venv/bin/black --check src`, `.venv/bin/ruff check src`.
- If a test fails in a file you do not own, stop and report it.

# Packet P33b — follow-up: refresh tests count the live sample

Packet P33 (already applied, uncommitted) taught the availability reader the `OpenCode/Go` label and extended the live-sample fixture from 9 to 12 subscription entries (three OpenCode Go buckets). `ai-subs.sh` really does emit 12 buckets now. Two tests in `tests/test_refresh_availability.py` still assert 9.

## Allowed files (touch nothing else)
- `tests/test_refresh_availability.py`

## Contract
1. Update the two assertions (`test_dry_run_prints_json_and_writes_nothing`, `test_the_live_sample_still_passes_validation`) from 9 to 12 entries; keep every other assertion.
2. Add one assertion to `test_the_live_sample_still_passes_validation` that the sample contains exactly 3 entries with provider `OpenCode/Go`, so the count is explained, not magic.
3. Done: full suite green (expect 569); paste the last line of pytest.
