# Packet P5-2 — `lee-llm-router evidence ladders --derive`

- Kind: `impl`
- Declared size: 4 files, at most 500 changed lines
- Owned paths: `src/lee_llm_router/staffing/ladder_derivation.py` (new),
  `src/lee_llm_router/doctor.py` (new evidence-ladders subparser and handler only),
  `tests/test_staffing_ladder_derivation.py` (new),
  `tests/test_doctor_evidence_ladders.py` (new)
- Requirement: read `docs/staffing/phase5-contracts.md` §3 and §6 before starting. Add
  `lee-llm-router evidence ladders --derive [--json] [--catalog-dir PATH] [--output-dir PATH]`.
  For each observed `class_key` in the attempt ledger (via `rollup.rollup_ledger()`/
  `build_rollup()` — reuse it exactly as `evidence_report.py` does, do not re-implement
  grouping), rank every route with `comparison_eligible: true` for that class (n >=
  `rollup.MINIMUM_SAMPLE_SIZE`, i.e. n>=5) cheapest-first by mean `cost.usd_marginal` among
  verified-success attempts, breaking ties by higher `verified_pass/attempts`. Write the
  result to `config/staffing/derived-ladders-<YYYY-MM-DD>.json` (today's date; `--output-dir`
  overrides the directory, default `config/staffing`), a new additive file — do not touch
  `escalation-ladders.json`'s schema or any `config/staffing/*.yaml`. Shape: a JSON object
  with `schema_version` (a new literal you define, e.g. `"derived-ladder/1"`),
  `derived_at` (ISO date), and `classes`: a list of `{class_key, rungs: [{route_id, n,
  verified_pass, pass_rate, mean_cost_usd_marginal}], evidence_status}`, where
  `evidence_status` is `"derived"` when at least one route in the class has n>=5, or
  `"insufficient evidence, hand ladder retained"` (verbatim) when none does — in that case
  `rungs` is an empty list, never a guessed order. Then diff the derived ladders against
  both hand-authored sources per contract §3: (1)
  `ai-workforce-benchmark/config/escalation-ladders.json`'s `ladders[].rungs[].worker.
  model_family` (join via `role_aliases`/`model_aliases` — a route's `model` maps to a
  `model_family` through `model_aliases`, e.g. `claude-sonnet-5` -> `sonnet-5`; a route with
  no alias entry is reported as `unmapped`, never guessed), and (2) each named crew's
  `escalation_ladder` field in `config/staffing/crews.yaml` (a flat list of route ids —
  compare directly, no family mapping needed there). Print a text diff naming, per class or
  crew: whether the derived cheapest-first order agrees, and where it disagrees, which rung
  moved and why (its n/pass_rate/cost). Retire nothing — this command only prints the diff
  and writes the derived file; it never edits `escalation-ladders.json` or `crews.yaml`.
  `doctor.py`'s handler follows the `_run_price`/`_run_route_show` pattern for
  `--catalog-dir` resolution and the `fail(message)` -> stderr `"evidence ladders: <reason>"`
  / exit 3 convention (an invalid `--catalog-dir` is a caller error and hard-fails, matching
  P5-1's fix — see `docs/staffing/phase5-execution-log.md` "P5-1" for that exact precedent).
- Oracle: `.venv/bin/pytest -q tests/test_staffing_ladder_derivation.py tests/test_doctor_evidence_ladders.py`
- Review: independent review required after the oracle passes.
- Commit: `feat(staffing P5): add evidence ladders --derive command`

## Forbidden paths

Any file not listed in Owned paths above, including `rollup.py`, `evidence_report.py`,
`ladder.py`, any `config/staffing/*.yaml`, `config/staffing/derived-ladders-*.json` from a
prior run, `ai-workforce-benchmark/config/escalation-ladders.json`, or any other CLI
subcommand's code path in `doctor.py` beyond adding the new `evidence ladders` subparser and
its handler function.

## Required evidence

- Full output of the Oracle command above (all tests passing), including at least one test
  case each for: a class with n>=5 for exactly one route (derived, one rung), a class with
  n>=5 for two routes with different mean marginal cost (cheapest ranked first), a class with
  no route at n>=5 (`"insufficient evidence, hand ladder retained"`, empty rungs), and one
  diff case each against a benchmark `escalation-ladders.json` ladder and a `crews.yaml`
  `escalation_ladder` list (agreement and disagreement).
- Full suite: `.venv/bin/pytest -q` (report exact pass/fail counts).
- `.venv/bin/black --check` and `.venv/bin/ruff check` on every touched file.
- One live invocation of `lee-llm-router evidence ladders --derive` against the real
  committed ledger, catalog, and `ai-workforce-benchmark/config/escalation-ladders.json`,
  both `--json` and text mode, pasted into your report, plus confirmation the written
  `config/staffing/derived-ladders-<date>.json` file is valid JSON.

## Stop / escalation condition

Stop and report back without guessing if a route id observed in the ledger has no entry in
`config/staffing/routes.yaml` (an unknown route — do not invent a model/harness/channel for
it), or if `ai-workforce-benchmark/config/escalation-ladders.json`'s `model_aliases` has no
entry for a route's model (report that rung as `unmapped` in the diff, never guess a family).
