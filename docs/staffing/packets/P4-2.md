# Packet P4-2 — crew reorder + policy ordering rule (D215 rulings 1 and 3)

- Kind: `impl`
- Declared size: 5 files, at most 150 changed lines
- Owned paths: `config/staffing/crews.yaml`, `config/staffing/policy.yaml`,
  `config/staffing/schema/policy.schema.json`, `src/lee_llm_router/staffing/catalog.py`,
  `tests/test_staffing_policy.py` (or the existing policy test file — find and extend it,
  do not create a duplicate; search `tests/` for the file that already tests
  `PolicyCatalog`/`policy.schema.json` loading)
- Requirement, exactly two additive changes:

  1. **Crew reorder (D215 ruling 1, verbatim):** "Interactive crew `sol-low-glm-pi` reverts
     to Lee's original order, prepaid first: `impl` [`glm-5.3-flash | pi | opencode-go`,
     `z-ai/glm-5.3-flash | pi | openrouter`]; the D204 round-14 reorder is superseded for
     interactive use." In `config/staffing/crews.yaml`, find the `sol-low-glm-pi` crew's
     `worker_routes.impl` array (currently
     `[pi-z-ai-glm-5-3-flash-openrouter, pi-glm-5-3-flash-opencode-go]`) and swap it to
     `[pi-glm-5-3-flash-opencode-go, pi-z-ai-glm-5-3-flash-openrouter]` (opencode-go/prepaid
     first, openrouter/metered second). Do not touch any other crew or any other field of
     this crew. Add a one-line comment above the crew citing D215 ruling 1 for why the D204
     round-14 order (openrouter first) no longer applies to this interactive crew.

  2. **Policy ordering rule (D215 ruling 3, verbatim):** "Named-crew ordering rule, recorded
     in `policy.yaml`: within a role's array, order by marginal price at authoring time with
     prepaid-first as the tie-break; a human may pin otherwise with a stated reason." Add a
     new top-level `crew_ordering_rule` record to `config/staffing/policy.yaml`, following the
     exact style of the file's other policy records (each carries `decision` and `source`
     citing the exact decision id — see how `human_escalation_cost_usd`, `role_floors`, or
     `never_automatic` entries cite their decisions). This is recorded-data policy, like
     `role_floors` — a documented authoring rule, not new selection code; nothing in this
     packet computes or enforces route ordering at runtime. Add the matching `crew_ordering_rule`
     shape to `config/staffing/schema/policy.schema.json` (a single required object with
     `additionalProperties: false`, requiring nonempty `decision` and `source` strings — follow
     the schema file's existing `$defs` conventions, e.g. reuse `#/$defs/nonemptyString`), and
     wire the new field into `PolicyCatalog` in `src/lee_llm_router/staffing/catalog.py`
     (a new frozen dataclass plus one field on `PolicyCatalog`, following exactly how the
     existing fields — e.g. `role_floors` — are declared and built via `_build_tuple`/the
     loader, except this one is a single object, not a tuple of records, so build it directly
     without `_build_tuple`). Update every existing test fixture/golden policy document that
     the full suite loads (real `config/staffing/policy.yaml` plus any test-fixture policy
     YAML files that assert against the full schema) so they satisfy the new required field —
     search the test suite for other fixture policy YAML documents before running the oracle.

- Oracle: `PYTHONPATH=src .venv/bin/python -m pytest -q`
- Review: independent review required after the oracle passes.
- Commit: `feat(staffing P4): reorder sol-low-glm-pi and record crew ordering rule (D215)`

## Forbidden paths

Any file not listed in Owned paths above, except test fixture files you must touch only to
add the new required `crew_ordering_rule` field with the same recorded text (never to change
unrelated fixture data). If a fixture file is not listed in Owned paths but must be touched to
satisfy the new required schema field, add it to your report explicitly rather than silently
expanding scope.

## Required evidence

- Full suite: `PYTHONPATH=src .venv/bin/python -m pytest -q` (report exact pass/fail counts —
  this must include the router's existing live-snapshot invariant tests, which re-validate the
  real committed `crews.yaml`/`policy.yaml` against the schema).
- `.venv/bin/black --check` and `.venv/bin/ruff check` on every touched `.py` file.
- `PYTHONPATH=src .venv/bin/python -m lee_llm_router.doctor doctor --catalog config/staffing`
  (or the equivalent catalog-validation doctor invocation already used elsewhere in this
  repo — find the exact command from `docs/staffing/phase0-contracts.md` or existing doctor
  tests) exits 0 on the real committed catalog.
- One live invocation of `lee-llm-router staff --mode crew sol-low-glm-pi --json` showing
  `worker_routes.impl[0]` is now `pi-glm-5-3-flash-opencode-go`.

## Stop / escalation condition

Stop and report back without guessing if `PolicyCatalog`'s existing `_build_tuple` loader
pattern cannot cleanly accommodate a single (non-array) new field without a larger refactor,
or if any committed test fixture policy document cannot be minimally updated to add the new
required field without changing its other recorded content.
