# Packet A1 — Text explain tier-kind parity

- Kind: `impl`
- Declared size: 2 files, at most 80 changed lines
- Owned paths: `src/lee_llm_router/doctor.py`,
  `tests/test_staffing_catalog_explain.py`
- Requirement: the human-readable `catalog explain` terms disclosure includes
  each effective channel tier's `kind`, matching the already-authoritative
  JSON terms view. Preserve all existing JSON fields and selection behavior.
- Oracle: `.venv/bin/pytest -q tests/test_staffing_catalog_explain.py`
- Review: independent review required after the oracle passes.
- Commit: `feat(staffing P3): expose tier kind in text explain`

## Forbidden paths

- Any file not listed in Owned paths above, including any other
  `src/lee_llm_router/staffing/*` module. If the JSON terms view's `kind`
  field is computed elsewhere and merely needs to be read and rendered in
  `doctor.py`'s text path, do not modify where it is computed.

## Required evidence

Report the full output of the Oracle command above (all tests passing),
plus `.venv/bin/black --check` and `.venv/bin/ruff check` on
`src/lee_llm_router/doctor.py`, plus a sample `catalog explain` text-mode
invocation showing a tier's `kind` alongside its other disclosed terms, and
the same invocation with `--json` showing the field was already present and
unchanged there.

## Stop / escalation condition

Stop and report back without guessing if the JSON terms view does not
already expose a `kind` field per effective channel tier, if there is no
single unambiguous place in the existing text-rendering path to add it
without changing existing text output for unrelated fields, or if adding it
would require touching a file outside Owned paths.
