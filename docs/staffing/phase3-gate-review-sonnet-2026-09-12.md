## Phase 3 Final Gate Review — `git diff 34cf0ee..HEAD`

**Scope covered:** phase3-contracts.md, D213 rulings 1–8, D209 doctrine (usage truth, Rule B, review classification, genuine stops), D206 class-metadata prohibition, against the full committed diff (35 files, +7343/-330), the acceptance commits (`6631b02`, `1aa64cd`, `0f1e5d9`), and the foreground-dispatch fix (`7860ba8`). Verified against the source doctrine at `/home/lee/projects/governed-execution-engine-docs/docs/supervised-agent-operating-doctrine.md` and D213/D209/D206 in `chief-of-staff/decisions.md`. Full test suite run: `1660 passed, 1 skipped` (the skip is a pre-existing, unrelated `rfc3339-validator` environment gap).

### Checked items — all confirmed compliant

1. **`/supervise` hard command boundary** (`supervise.md.tmpl`): the numbered loop calls only `staff`, `run`, `classify-failure`, `next-action`, `census`, `evidence rollup`; explicitly forbids provider binaries. Enforced by `test_shims_parity.py::test_supervise_bodies_are_parity_checked_against_d213`, which parses every `lee-llm-router <cmd>` token from the rendered body and asserts equality with the six-command set.
2. **Rules A–K quotes**: every quoted sentence in the template is byte-identical to the source doctrine (verified line-by-line against the doctrine file). Not paraphrased.
3. **Class metadata never maps to a model/route** (D206): `derive_class.py` derives only `role/oracle_type/domain_tags/size_band/language` from packet facts (kind, owned-path extensions, declared size, named oracle/reviewer, keyword table); `staff --from-packet` (doctor.py) feeds only the class key into the normal staffing service, never a route/model directly. Domain tags come only from an explicit `Domain:` field or literal owned-path keyword matches (P3-7-3 fix correctly closed the prose-substring over-tagging bug found by the acceptance run).
4. **`classify-failure`**: `spec_rejected`/`capability_rejected` are reachable only via `_validate_judgment`, which requires an explicit `--judgment` value from the closed two-value set plus a non-"pass" review verdict; CLI wiring in doctor.py passes `--judgment`/`--review-verdict` straight through with no inference path.
5. **`run` refuses intersecting owned paths**: `census.py::register_run` atomically purges stale records then checks path intersection under an `fcntl` flock before writing; `doctor.py`'s `run` command registers before dispatch and deregisters on every exit path (finally-block).
6. **`census` cleans stale pids with a note**: `_purge_stale_locked`/`evaluate_record_liveness` distinguish dead-pid vs. pid-reuse (Linux start-time comparison) and report a reason per cleaned record.
7. **Acceptance commits touched only their declared owned files**: `6631b02` → `doctor.py` + `test_staffing_catalog_explain.py`; `1aa64cd` → `ledger.py`, `import_evidence.py`, both import test files; `0f1e5d9` → `test_staffing_ledger.py` only. Exact match to A1/A2a/A2b's declared owned paths in `phase3-acceptance-plan.md`. `.gitignore` untouched in the final diff.
8. **A2a's multi-process regression** calls the real `import_benchmark_evidence`/`import_agent_orch_evidence` directly via `fork`+barrier (no surrogate), and both real import functions wrap their entire read-decide-append sequence in `with writer_transaction(target):`. A2b removes exactly the two named surrogate workers/tests and rewrites the primitive-lock test; no other test in the file changed.
9. **Foreground-dispatch fix (`7860ba8`)**: the "## Foreground execution" section is present verbatim as committed, pinned by parity-test substring assertions.
10. **No invented usage/cost**: the new subscription-cost exception (`fefd714`) only relaxes the `cost.basis == ["unavailable"]` case, and only when `channel_kind == "subscription"` (read from committed `channels.yaml`, never guessed); metered/`None`/unrecognized channels stay fail-closed. The schema change removing the redundant `router_run.cost` `allOf` was verified against `$defs/cost`, which already enforces `usd_list`/`usd_marginal` whenever `basis == ["list","marginal"]` — no requirement was actually weakened. No hardcoded dollar/token literals found anywhere in the `src/` diff.

### Non-blocking finding

- **`docs/staffing/needs-lee.md` is stale.** Its only Phase 3 entry ("blocking after the authorized P3-6 rerun") still describes an unresolved blocker and asks Lee to authorize another acceptance session — but `chief-answers-p3-1.md`/`chief-answers-p3-2.md` already authorized the second and third reruns, and `phase3-execution-log.md` documents that both acceptance items landed and were reviewed (`6631b02`, `1aa64cd`, `0f1e5d9`). This isn't a violation of any D213 ruling — the actual authorizations happened correctly elsewhere — but the file itself was never updated to reflect resolution, which could mislead the next reader (or Chief, ahead of sealing D214) into thinking Phase 3 is still blocked. Recommend updating/closing this entry before D214.

### Future concern (already self-flagged, noted for completeness)

- `phase3-execution-log.md`'s own "Observation for a future session" notes that the crew's review-ladder route (`pi-deepseek-v4-pro-opencode-go` → `pi-deepseek-deepseek-v4-flash-openrouter`) reported no shell/bash access during A2a/A2b review, unlike earlier P1–P3 sessions on the same route — a possible harness/environment regression worth investigating, outside this phase's scope since the supervisor's own personal verification already covered the gap.

**VERDICT: PASS**
